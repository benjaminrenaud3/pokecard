{
    "name": "my-app",
    "version": "1.0.0",
    "private": true,
    "engines": {
        "node": "14.x"
    },
    "scripts": {
        "build": "cd client && npm run build",
        "install": "cd client && npm install && cd ../react-node-app && npm install",
        "start": "cd react-node-app && node server/index.js && cd ../client && npm start"
    }
}
