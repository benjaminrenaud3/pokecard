{
    "name": "my-app",
    "version": "1.0.0",
    "private": true,
    "engines": {
        "node": "14.x"
    },
    "scripts": {
        "postinstall": "cd client && npm install && npm run build && cd ../react-node-app && npm install",
        "start": "cd react-node-app && npm start && cd ../client && npm start"
    }
}
