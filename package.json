{
    "name": "my-app",
    "version": "1.0.0",
    "private": true,
    "engines": {
        "node": "14.x"
    },
    "scripts": {
        "build": "cd frontend && npm run build",
        "install": "cd frontend && npm install && cd ../backend && npm install",
        "start": "cd backend && node server/index.js && cd ../frontend && npm start"
    }
}
